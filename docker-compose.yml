services:
  media-library:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/data
      - /path/to/your/videos:/media:ro
    environment:
      - DB_PATH=/data/library.db
    restart: unless-stopped
