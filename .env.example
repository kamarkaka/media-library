# Server
PORT=3000
DB_PATH=./data/library.db

# Auth — run `npm run setup-auth` to generate these
AUTH_USERNAME=admin
AUTH_PASSWORD_HASH=
SESSION_SECRET=

# Scraper — default: noop (no metadata scraping)
SCRAPER_TYPE=noop
