# Server
PORT=3000
DB_PATH=./data/library.db

# Scraper — default: noop (no metadata scraping)
SCRAPER_TYPE=noop

# Auth credentials are stored in the database.
# Run `npm run setup-auth` to set username and password.
